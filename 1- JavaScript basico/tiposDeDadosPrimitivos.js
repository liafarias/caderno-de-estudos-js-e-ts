// Stings
const nome = "Luana";
const sobrenome = 'Lopes';
const naturalidade = `Brasileira`;

// Numbers
const idade = 17;
const altura = 1.60; //ponto flutuante

// Undefined e Null
let nomeAluno; // não aponta para local nenhum na memória, pois será declarada logo mais no codigo, por isso sempre se usa let
const sobrenomeAluno = null; // não aponta para local nenhum na memória, utilizamos quando queremos desconfigurar a variavel, ou seja ela não tem nenhum valor.

/*OBS: Quando utilizamos o nulo afirmamos que não queremos que essa variavel não aponte para nenhum valor na memoria, já quando não declaramos um valor para a variavel ela é undefined */

// Boolean
const aprovado = true; // verdadeiro
const reprovado = false; // falso

/* Valores logicos, para mudar o fluxo de uma operação */

console.log(typeof nomeDaVariavel); //imprimir o typeof que é utilizado para saber o tipo de dado declarado
console.log(nome) // para imprimir apenas o valor do declarado.

// Symbol